# Nord City — единый .env в корне проекта (nord-city-project/.env)
# Используется: infrastructure (orchestrator, bot, nginx), web (Next.js)
# Скопируйте в .env и заполните значения: cp .env.example .env

# === Telegram Bot ===
BOT_TOKEN=your_bot_token
ADMIN_CHAT_ID=your_admin_chat_id

# === Database (PostgreSQL) ===
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=nordcity_db
DB_USER=nordcity_app
DB_PASSWORD=your_password

# === Internal Services HTTP ===
# Bind internal services to 127.0.0.1 for security (not exposed to network)
DATABASE_SERVICE_HOST=127.0.0.1
DATABASE_SERVICE_HTTP_URL=http://127.0.0.1:8001
BOT_SERVICE_HOST=127.0.0.1
MEDIA_SERVICE_HOST=127.0.0.1
MEDIA_SERVICE_HTTP_URL=http://127.0.0.1:8004
# Public base for media URLs returned to clients (e.g. https://your-domain.com/api/v1)
PUBLIC_API_BASE_URL=https://your-domain.com/api/v1
WEB_SERVICE_PORT=8003

# === CORS (Web Service) ===
# Разрешённые origins для запросов к API. Через запятую.
CORS_ORIGINS=http://localhost:3000,https://your-domain.com

# === Nginx ===
NGINX_DOMAIN=your-domain.com
NGINX_SSL_CERT=/etc/letsencrypt/live/your-domain.com/fullchain.pem
NGINX_SSL_KEY=/etc/letsencrypt/live/your-domain.com/privkey.pem
SITE_PORT=3000

# === Frontend (Next.js) ===
# URL API для браузера. Обычно: https://<NGINX_DOMAIN>/api/v1
NEXT_PUBLIC_API_URL=https://your-domain.com/api/v1
